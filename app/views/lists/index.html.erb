<h1>Movie Lists</h1>

<div>
<% @lists.each do | list | %>
    <%= link_to list_path(list) do %>
       <%= list.name %>
  <% end %>
   <% end %>
</div>

<%= link_to 'Create new list', new_list_path %>
